[package]
name = "prune-backup"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
chrono = "0.4"
anyhow = "1"

[dev-dependencies]
filetime = "0.2"
tempfile = "3"
